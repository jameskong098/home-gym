import SwiftUI

struct ActivityView: View {
    var body: some View {
        VStack {
            Text("Activity Page")
                .font(.largeTitle)
                .fontWeight(.bold)
            // Add content for the activity page
        }
        .padding()
    }
}
